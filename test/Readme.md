To run Go in a terminal first initiate with 

`go mod init test`

Where test is the module that gets created (THere is some link between this and package that gets created in main.go file)

To do go run :

`go run .`

`go run main.go`